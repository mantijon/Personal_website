# Alex Doe Personal Website

A static React + Vite personal website built from the supplied Figma Make reference.

## Run locally

```bash
npm install
npm run dev
```

The site has three hash routes: `#home`, `#books`, and `#projects`. Content is editable in `src/data/`, while the local placeholder artwork lives in `public/covers/` and `public/projects/`.
